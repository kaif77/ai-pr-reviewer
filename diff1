diff --git a/src/App.jsx b/src/App.jsx
index 4eb5cf3..a5fc147 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -13,6 +13,7 @@ import PaymentCancelled from './pages/PaymentCancelled';
 import StudentDashboard  from './pages/student/Dashboard';
 import ApplicationWizard from './pages/student/ApplicationWizard';
 import ApplicationDetail from './pages/student/ApplicationDetail';
+import StudentProfile    from './pages/student/Profile';
 
 // Admin pages
 import AdminDashboard          from './pages/admin/Dashboard';
@@ -44,9 +45,10 @@ function App() {
         <Route path="/payment-cancelled" element={<PaymentCancelled />} />
 
         {/* Student */}
-        <Route path="/student/dashboard" element={<ProtectedRoute role="student"><StudentDashboard /></ProtectedRoute>} />
-        <Route path="/student/apply"     element={<ProtectedRoute role="student"><ApplicationWizard /></ProtectedRoute>} />
+        <Route path="/student/dashboard"        element={<ProtectedRoute role="student"><StudentDashboard /></ProtectedRoute>} />
+        <Route path="/student/apply"            element={<ProtectedRoute role="student"><ApplicationWizard /></ProtectedRoute>} />
         <Route path="/student/applications/:id" element={<ProtectedRoute role="student"><ApplicationDetail /></ProtectedRoute>} />
+        <Route path="/student/profile"          element={<ProtectedRoute role="student"><StudentProfile /></ProtectedRoute>} />
 
         {/* Admin — all admins */}
         <Route path="/admin/dashboard"       element={<ProtectedRoute role="admin"><AdminDashboard /></ProtectedRoute>} />
diff --git a/src/layouts/AdminLayout.jsx b/src/layouts/AdminLayout.jsx
index 1fc3b9e..12f082a 100644
--- a/src/layouts/AdminLayout.jsx
+++ b/src/layouts/AdminLayout.jsx
@@ -91,7 +91,7 @@ const AdminLayout = ({ title, children }) => {
           <p className="text-xs font-semibold tracking-wider uppercase mb-3" style={{ color: PRIMARY }}>
             Superadmin
           </p>
-        )}
+        ) || ''}
         <button
           onClick={handleLogout}
           className="text-xs font-semibold tracking-wide transition-opacity"
diff --git a/src/layouts/StudentLayout.jsx b/src/layouts/StudentLayout.jsx
index f31cf61..a1be209 100644
--- a/src/layouts/StudentLayout.jsx
+++ b/src/layouts/StudentLayout.jsx
@@ -1,68 +1,200 @@
-import { Link } from 'react-router-dom';
+import { useState, useEffect } from 'react';
+import { Link, useLocation } from 'react-router-dom';
 import { getCurrentUser, logout } from '../services/auth';
-import { INK, BORDER, PRIMARY, PRIMARY_LIGHT } from '../utils/theme';
-
-/**
- * Shared layout for all student-facing pages.
- * Props:
- *   title    – page title shown as breadcrumb after the wordmark
- *   subtitle – optional secondary label (e.g. application number)
- *   children – page content
- */
-const StudentLayout = ({ title, subtitle, children }) => {
+import { INK, BORDER, PRIMARY, PRIMARY_LIGHT, WHITE } from '../utils/theme';
+
+// ─── SVG Icons ────────────────────────────────────────────────────────────────
+const Icon = {
+  dashboard: (
+    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+      <rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/>
+      <rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/>
+    </svg>
+  ),
+  apply: (
+    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+      <circle cx="12" cy="12" r="9"/><path d="M12 8v8M8 12h8"/>
+    </svg>
+  ),
+  profile: (
+    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+      <circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/>
+    </svg>
+  ),
+  logout: (
+    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+      <path d="M17 16l4-4-4-4M21 12H9"/><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/>
+    </svg>
+  ),
+  menu: (
+    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
+      <path d="M4 6h16M4 12h16M4 18h16"/>
+    </svg>
+  ),
+  close: (
+    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
+      <path d="M18 6L6 18M6 6l12 12"/>
+    </svg>
+  ),
+};
+
+const NAV_ITEMS = [
+  { path: '/student/dashboard', icon: 'dashboard', label: 'Dashboard' },
+  { path: '/student/apply',     icon: 'apply',     label: 'New Application' },
+  { path: '/student/profile',   icon: 'profile',   label: 'My Profile' },
+];
+
+// ─── Sidebar content (shared between desktop + mobile) ────────────────────────
+const SidebarContent = ({ onClose }) => {
+  const location = useLocation();
   const user = getCurrentUser();
 
   return (
-    <div
-      style={{ fontFamily: "'Inter', system-ui, sans-serif", color: INK }}
-      className="min-h-screen bg-white"
-    >
-      {/* ── Navbar ── */}
-      <nav
-        className="sticky top-0 z-50 flex items-center justify-between px-6 md:px-10 py-5"
-        style={{ backgroundColor: '#FFFFFF', borderBottom: BORDER }}
+    <div className="flex flex-col h-full" style={{ fontFamily: "'Inter', system-ui, sans-serif" }}>
+      {/* Logo */}
+      <div className="flex items-center justify-between px-6 py-5" style={{ borderBottom: BORDER }}>
+        <Link to="/student/dashboard" className="font-black text-sm tracking-tighter uppercase" style={{ color: PRIMARY }}>
+          COE<span className="font-light mx-1 opacity-40">·</span>INDEX
+        </Link>
+        {onClose && (
+          <button onClick={onClose} className="p-1 rounded opacity-50 hover:opacity-100 transition-opacity" style={{ color: INK }}>
+            {Icon.close}
+          </button>
+        )}
+      </div>
+
+      {/* Nav label */}
+      <p className="px-6 pt-5 pb-2 text-[10px] font-bold tracking-[0.2em] uppercase opacity-30">Navigation</p>
+
+      {/* Nav items */}
+      <nav className="flex-1 px-3 space-y-0.5">
+        {NAV_ITEMS.map(item => {
+          const isActive = location.pathname === item.path ||
+            (item.path !== '/student/dashboard' && item.path !== '/student/apply' && location.pathname.startsWith(item.path));
+          return (
+            <Link
+              key={item.path}
+              to={item.path}
+              className="flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-colors"
+              style={{
+                color: isActive ? PRIMARY : `${INK}80`,
+                backgroundColor: isActive ? PRIMARY_LIGHT : 'transparent',
+              }}
+              onMouseEnter={e => { if (!isActive) { e.currentTarget.style.backgroundColor = '#F8FAFC'; e.currentTarget.style.color = INK; } }}
+              onMouseLeave={e => { if (!isActive) { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = `${INK}80`; } }}
+            >
+              <span style={{ color: isActive ? PRIMARY : `${INK}50` }}>{Icon[item.icon]}</span>
+              <span>{item.label}</span>
+              {isActive && (
+                <span className="ml-auto w-1.5 h-4 rounded-full" style={{ backgroundColor: PRIMARY }} />
+              )}
+            </Link>
+          );
+        })}
+      </nav>
+
+      {/* User section */}
+      <div className="px-3 py-4" style={{ borderTop: BORDER }}>
+        <Link
+          to="/student/profile"
+          className="flex items-center gap-3 px-3 py-2.5 rounded-lg mb-1 transition-colors"
+          style={{ color: INK }}
+          onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY_LIGHT; }}
+          onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; }}
+        >
+          <div
+            className="w-8 h-8 rounded-full flex items-center justify-center text-xs font-black text-white shrink-0"
+            style={{ backgroundColor: PRIMARY }}
+          >
+            {user?.full_name?.charAt(0)?.toUpperCase() || 'S'}
+          </div>
+          <div className="min-w-0">
+            <p className="text-sm font-semibold truncate leading-tight">{user?.full_name || 'Student'}</p>
+            <p className="text-xs truncate leading-tight" style={{ color: `${INK}45` }}>{user?.email}</p>
+          </div>
+        </Link>
+        <button
+          onClick={logout}
+          className="flex items-center gap-2.5 w-full px-3 py-2.5 rounded-lg text-xs font-medium transition-colors"
+          style={{ color: `${INK}50` }}
+          onMouseEnter={e => { e.currentTarget.style.backgroundColor = '#FEF2F2'; e.currentTarget.style.color = '#DC2626'; }}
+          onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = `${INK}50`; }}
+        >
+          {Icon.logout}
+          Sign out
+        </button>
+      </div>
+    </div>
+  );
+};
+
+// ─── Layout ───────────────────────────────────────────────────────────────────
+const StudentLayout = ({ children }) => {
+  const [mobileOpen, setMobileOpen] = useState(false);
+  const location = useLocation();
+
+  // Close mobile sidebar on route change
+  useEffect(() => { setMobileOpen(false); }, [location.pathname]);
+
+  return (
+    <div className="flex min-h-screen" style={{ fontFamily: "'Inter', system-ui, sans-serif", color: INK, backgroundColor: '#F8FAFC' }}>
+
+      {/* ── Desktop sidebar (sticky) ── */}
+      <aside
+        className="hidden lg:flex flex-col shrink-0 sticky top-0 h-screen overflow-y-auto"
+        style={{ width: '256px', backgroundColor: WHITE, borderRight: BORDER }}
       >
-        {/* Left: wordmark + breadcrumb */}
-        <div className="flex items-center gap-3 min-w-0">
-          <Link
-            to="/student/dashboard"
-            className="font-black text-sm tracking-tighter uppercase shrink-0 hover:opacity-70 transition-opacity"
-            style={{ color: PRIMARY }}
+        <SidebarContent />
+      </aside>
+
+      {/* ── Mobile overlay sidebar ── */}
+      {mobileOpen && (
+        <div className="lg:hidden fixed inset-0 z-50 flex">
+          <div
+            className="absolute inset-0"
+            style={{ backgroundColor: 'rgba(0,0,0,0.45)' }}
+            onClick={() => setMobileOpen(false)}
+          />
+          <aside
+            className="relative flex flex-col overflow-y-auto"
+            style={{ width: '256px', backgroundColor: WHITE, borderRight: BORDER }}
           >
-            COE<span className="font-light mx-1 opacity-40">·</span>INDEX
-          </Link>
-          {title && (
-            <>
-              <span className="opacity-30 shrink-0">/</span>
-              <span className="text-sm font-medium tracking-wide truncate">{title}</span>
-            </>
-          )}
-          {subtitle && (
-            <span className="hidden sm:block text-xs tracking-widest uppercase opacity-40 shrink-0">
-              {subtitle}
-            </span>
-          )}
+            <SidebarContent onClose={() => setMobileOpen(false)} />
+          </aside>
         </div>
+      )}
 
-        {/* Right: user name + logout */}
-        <div className="flex items-center gap-4 shrink-0">
-          <span className="hidden md:block text-sm opacity-50 tracking-wide">
-            {user?.full_name}
-          </span>
+      {/* ── Main content area ── */}
+      <div className="flex-1 flex flex-col min-w-0">
+
+        {/* Mobile top bar */}
+        <div
+          className="lg:hidden sticky top-0 z-30 flex items-center gap-3 px-4 py-3"
+          style={{ backgroundColor: WHITE, borderBottom: BORDER }}
+        >
           <button
-            onClick={logout}
-            className="text-xs px-4 py-2 font-semibold tracking-wide transition-colors hover:text-white"
-            style={{ border: `1px solid ${PRIMARY}`, color: PRIMARY, borderRadius: '8px' }}
-            onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY; e.currentTarget.style.color = '#fff'; }}
-            onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = PRIMARY; }}
+            onClick={() => setMobileOpen(true)}
+            className="p-2 rounded-lg transition-colors"
+            style={{ color: INK }}
+            onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY_LIGHT; }}
+            onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; }}
           >
-            Logout
+            {Icon.menu}
           </button>
+          <Link
+            to="/student/dashboard"
+            className="font-black text-sm tracking-tighter uppercase"
+            style={{ color: PRIMARY }}
+          >
+            COE<span className="font-light mx-1 opacity-40">·</span>INDEX
+          </Link>
         </div>
-      </nav>
 
-      {/* ── Page content ── */}
-      {children}
+        {/* Page content */}
+        <main className="flex-1">
+          {children}
+        </main>
+      </div>
     </div>
   );
 };
diff --git a/src/pages/student/ApplicationDetail.jsx b/src/pages/student/ApplicationDetail.jsx
index 8b1ae43..81151e2 100644
--- a/src/pages/student/ApplicationDetail.jsx
+++ b/src/pages/student/ApplicationDetail.jsx
@@ -6,49 +6,207 @@ import StudentLayout from '../../layouts/StudentLayout';
 import { WHITE, INK, BORDER, PRIMARY, PRIMARY_LIGHT } from '../../utils/theme';
 import { STATUS_COLORS, DOCUMENT_TYPES } from '../../utils/constants';
 
-// ─── Status badge ─────────────────────────────────────────────────────────────
-const STATUS_TEXT_COLOR = {
-  pending: 'text-amber-700',
-  info:    'text-sky-700',
-  warning: 'text-orange-700',
-  success: 'text-emerald-700',
-  error:   'text-red-700',
+// ─── Status config ─────────────────────────────────────────────────────────────
+const STATUS_STYLE = {
+  pending: { bg: '#FFFBEB', border: '#FCD34D', text: '#92400E' },
+  info:    { bg: '#F0F9FF', border: '#7DD3FC', text: '#075985' },
+  warning: { bg: '#FFF7ED', border: '#FDBA74', text: '#9A3412' },
+  success: { bg: '#F0FDF4', border: '#86EFAC', text: '#14532D' },
+  error:   { bg: '#FEF2F2', border: '#FCA5A5', text: '#7F1D1D' },
 };
 
 const StatusBadge = ({ status }) => {
   const variant = STATUS_COLORS[status] || 'info';
+  const s = STATUS_STYLE[variant];
   return (
     <span
-      className={`text-xs px-2.5 py-1 tracking-wider uppercase font-semibold ${STATUS_TEXT_COLOR[variant]}`}
-      style={{ border: '1px solid currentColor' }}
+      className="text-[10px] px-3 py-1.5 font-bold tracking-widest uppercase rounded-full"
+      style={{ backgroundColor: s.bg, border: `1px solid ${s.border}`, color: s.text }}
     >
       {status}
     </span>
   );
 };
 
-// ─── Info row ─────────────────────────────────────────────────────────────────
+// ─── Workflow timeline ─────────────────────────────────────────────────────────
+const TIMELINE_STEPS = [
+  { key: 'Awaiting Initial Payment', label: 'Payment',    icon: '💳' },
+  { key: 'Awaiting Admin Approval',  label: 'Review',     icon: '📋' },
+  { key: 'Submitted to Campus',      label: 'Campus',     icon: '🏫' },
+  { key: 'Offer Letter Received',    label: 'Offer',      icon: '📨' },
+  { key: 'Awaiting Final Approval',  label: 'Final',      icon: '✍️' },
+  { key: 'Completed',                label: 'Complete',   icon: '🎓' },
+];
+
+const ORDER = [
+  'Awaiting Initial Payment',
+  'Payment Completed',
+  'Awaiting Document Upload',
+  'Awaiting Admin Approval',
+  'Submitted to Campus',
+  'Offer Letter Received',
+  'Awaiting Final Approval',
+  'Completed',
+];
+
+const StatusTimeline = ({ status }) => {
+  if (status === 'Rejected') {
+    return (
+      <div className="flex items-center gap-3 px-4 py-3 rounded-xl" style={{ backgroundColor: '#FEF2F2', border: '1px solid #FCA5A5' }}>
+        <span className="text-xl">❌</span>
+        <div>
+          <p className="font-bold text-sm text-red-700">Application Rejected</p>
+          <p className="text-xs text-red-500 mt-0.5">Please contact support for more information.</p>
+        </div>
+      </div>
+    );
+  }
+
+  const currentOrder = ORDER.indexOf(status);
+
+  return (
+    <div className="flex items-center gap-1 overflow-x-auto pb-1">
+      {TIMELINE_STEPS.map((step, i) => {
+        const stepOrder = ORDER.indexOf(step.key);
+        const isDone    = currentOrder > stepOrder;
+        const isCurrent = ORDER.slice(stepOrder, stepOrder + 3).includes(status) && currentOrder <= stepOrder + 2 && !isDone;
+
+        return (
+          <div key={step.key} className="flex items-center gap-1 shrink-0">
+            <div className="flex flex-col items-center gap-1">
+              <div
+                className="w-9 h-9 rounded-full flex items-center justify-center text-sm font-bold transition-all"
+                style={{
+                  backgroundColor: isDone ? '#DCFCE7' : isCurrent ? PRIMARY_LIGHT : '#F8FAFC',
+                  border: isDone ? '2px solid #10B981' : isCurrent ? `2px solid ${PRIMARY}` : '2px solid #E2E8F0',
+                  color: isDone ? '#059669' : isCurrent ? PRIMARY : '#CBD5E1',
+                }}
+              >
+                {isDone ? '✓' : step.icon}
+              </div>
+              <span
+                className="text-[9px] font-semibold tracking-wide text-center"
+                style={{ color: isDone ? '#059669' : isCurrent ? PRIMARY : '#94A3B8' }}
+              >
+                {step.label}
+              </span>
+            </div>
+            {i < TIMELINE_STEPS.length - 1 && (
+              <div
+                className="w-6 h-0.5 mb-4 rounded-full shrink-0"
+                style={{ backgroundColor: isDone ? '#10B981' : '#E2E8F0' }}
+              />
+            )}
+          </div>
+        );
+      })}
+    </div>
+  );
+};
+
+// ─── Info row ──────────────────────────────────────────────────────────────────
 const InfoRow = ({ label, value }) => (
-  <div className="flex justify-between items-start py-3.5 gap-4" style={{ borderBottom: BORDER }}>
-    <p className="text-xs tracking-[0.12em] uppercase opacity-40 shrink-0 mt-0.5">{label}</p>
+  <div className="flex justify-between items-start py-3 gap-4" style={{ borderBottom: '1px solid #F1F5F9' }}>
+    <p className="text-xs tracking-wide uppercase opacity-40 shrink-0 mt-0.5">{label}</p>
     <p className="font-semibold text-sm text-right">{value || '—'}</p>
   </div>
 );
 
-// ─── Section wrapper ──────────────────────────────────────────────────────────
-const Section = ({ title, children }) => (
-  <div style={{ border: BORDER }}>
-    <div className="px-6 py-4" style={{ backgroundColor: PRIMARY_LIGHT, borderBottom: BORDER }}>
-      <h2 className="font-bold text-sm tracking-[0.15em] uppercase" style={{ color: PRIMARY }}>{title}</h2>
+// ─── Section card ──────────────────────────────────────────────────────────────
+const Card = ({ title, children }) => (
+  <div className="rounded-2xl overflow-hidden" style={{ backgroundColor: WHITE, border: '1px solid #E2E8F0', boxShadow: '0 1px 4px rgba(0,0,0,0.05)' }}>
+    <div className="px-6 py-4" style={{ backgroundColor: '#FAFAFA', borderBottom: '1px solid #F1F5F9' }}>
+      <h2 className="font-bold text-xs tracking-[0.15em] uppercase" style={{ color: `${INK}60` }}>{title}</h2>
     </div>
     <div className="px-6">{children}</div>
   </div>
 );
 
-// ─── Page ─────────────────────────────────────────────────────────────────────
+// ─── Action panel ──────────────────────────────────────────────────────────────
+const ACTION_CONFIG = {
+  'Awaiting Initial Payment': {
+    type: 'payment',
+    color: PRIMARY,
+    bg: PRIMARY_LIGHT,
+    icon: '💳',
+    title: 'Initial Payment Required',
+    message: 'Pay LKR 50,000 to start your application process.',
+    primaryLabel: 'Pay LKR 50,000',
+  },
+  'Payment Completed': {
+    type: 'download',
+    color: '#0EA5E9',
+    bg: '#F0F9FF',
+    icon: '📄',
+    title: 'Download & Sign Your Form',
+    message: 'Download your application form, print and sign it, then upload with your documents.',
+    primaryLabel: 'Download Application Form',
+    secondaryLabel: 'Upload Documents',
+  },
+  'Awaiting Document Upload': {
+    type: 'upload',
+    color: PRIMARY,
+    bg: PRIMARY_LIGHT,
+    icon: '📤',
+    title: 'Upload Required',
+    message: 'Please upload your signed form and supporting documents.',
+    primaryLabel: 'Upload Documents',
+  },
+  'Awaiting Admin Approval': {
+    type: 'info',
+    color: '#6366F1',
+    bg: '#EEF2FF',
+    icon: '⏳',
+    title: 'Under Review',
+    message: 'Your documents are being reviewed by our admin team. We\'ll notify you once approved.',
+  },
+  'Submitted to Campus': {
+    type: 'info',
+    color: '#0EA5E9',
+    bg: '#F0F9FF',
+    icon: '🏫',
+    title: 'Submitted to Campus',
+    message: 'Your application has been forwarded to the campus. Waiting for their offer letter.',
+  },
+  'Offer Letter Received': {
+    type: 'final-payment',
+    color: '#059669',
+    bg: '#F0FDF4',
+    icon: '🎉',
+    title: 'Offer Letter Received!',
+    message: 'Congratulations! You have received an offer letter. Make your final payment to confirm your place.',
+    primaryLabel: 'Pay Final Fee (LKR 100,000)',
+  },
+  'Awaiting Final Approval': {
+    type: 'info',
+    color: '#6366F1',
+    bg: '#EEF2FF',
+    icon: '✍️',
+    title: 'Final Review',
+    message: 'Your final payment is being confirmed and processed. Almost there!',
+  },
+  'Completed': {
+    type: 'success',
+    color: '#059669',
+    bg: '#F0FDF4',
+    icon: '🎓',
+    title: 'Application Completed!',
+    message: 'Congratulations! Your application process is complete. Best of luck on your journey!',
+  },
+  'Rejected': {
+    type: 'error',
+    color: '#DC2626',
+    bg: '#FEF2F2',
+    icon: '❌',
+    title: 'Application Rejected',
+    message: 'Unfortunately your application was not approved. Please contact our support team for more information.',
+  },
+};
+
+// ─── Page ──────────────────────────────────────────────────────────────────────
 const ApplicationDetail = () => {
-  const { id } = useParams();
-  const navigate = useNavigate();
+  const { id }     = useParams();
+  const navigate   = useNavigate();
 
   const [application, setApplication]     = useState(null);
   const [loading, setLoading]             = useState(true);
@@ -56,17 +214,10 @@ const ApplicationDetail = () => {
   const [actionLoading, setActionLoading] = useState(false);
 
   useEffect(() => {
-    const fetchApplication = async () => {
-      try {
-        const response = await getApplicationById(id);
-        setApplication(response.data);
-      } catch {
-        setError('Failed to load application details.');
-      } finally {
-        setLoading(false);
-      }
-    };
-    fetchApplication();
+    getApplicationById(id)
+      .then(r => setApplication(r.data))
+      .catch(() => setError('Failed to load application details.'))
+      .finally(() => setLoading(false));
   }, [id]);
 
   const handlePayment = async (paymentType) => {
@@ -93,7 +244,7 @@ const ApplicationDetail = () => {
       a.click();
       URL.revokeObjectURL(url);
     } catch {
-      setError('Failed to download application form. Please try again.');
+      setError('Failed to download application form.');
     } finally {
       setActionLoading(false);
     }
@@ -103,47 +254,27 @@ const ApplicationDetail = () => {
     window.open(`http://localhost:5000/${filePath}`, '_blank');
   };
 
-  const PrimaryBtn = ({ onClick, disabled, children }) => (
-    <button
-      onClick={onClick}
-      disabled={disabled}
-      className="w-full py-4 text-sm font-semibold tracking-wide transition-opacity hover:opacity-80 disabled:opacity-50"
-      style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '8px' }}
-    >
-      {children}
-    </button>
-  );
-
-  const OutlineBtn = ({ onClick, children }) => (
-    <button
-      onClick={onClick}
-      className="w-full py-3 text-sm font-medium tracking-wide transition-colors"
-      style={{ border: `1px solid ${PRIMARY}`, color: PRIMARY, opacity: 0.7, borderRadius: '8px' }}
-      onMouseEnter={e => { e.currentTarget.style.opacity = 1; e.currentTarget.style.backgroundColor = PRIMARY_LIGHT; }}
-      onMouseLeave={e => { e.currentTarget.style.opacity = 0.7; e.currentTarget.style.backgroundColor = 'transparent'; }}
-    >
-      {children}
-    </button>
-  );
-
+  // ── Loading state ──
   if (loading) {
     return (
-      <StudentLayout title="Application">
-        <div className="flex items-center justify-center py-32">
+      <StudentLayout>
+        <div className="flex items-center justify-center min-h-96">
           <div className="text-center">
-            <div className="inline-block w-8 h-8 border-2 animate-spin" style={{ borderColor: PRIMARY, borderTopColor: 'transparent', borderRadius: '50%' }} />
-            <p className="mt-4 text-sm opacity-50">Loading application…</p>
+            <div className="w-8 h-8 border-2 border-t-transparent rounded-full animate-spin mx-auto mb-4"
+              style={{ borderColor: PRIMARY, borderTopColor: 'transparent' }} />
+            <p className="text-sm opacity-40">Loading application…</p>
           </div>
         </div>
       </StudentLayout>
     );
   }
 
+  // ── Error state ──
   if (error && !application) {
     return (
-      <StudentLayout title="Application">
-        <div className="flex items-center justify-center py-32 px-6">
-          <div className="px-4 py-3 text-sm max-w-sm w-full" style={{ border: '1px solid #DC2626', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
+      <StudentLayout>
+        <div className="flex items-center justify-center min-h-96 px-6">
+          <div className="px-5 py-4 rounded-xl text-sm max-w-sm w-full" style={{ border: '1px solid #FCA5A5', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
             {error}
           </div>
         </div>
@@ -151,139 +282,185 @@ const ApplicationDetail = () => {
     );
   }
 
+  const actionCfg = ACTION_CONFIG[application?.status] || {};
+
   return (
-    <StudentLayout title={application?.campus_name} subtitle={application?.application_unique_number}>
-      <div className="px-6 md:px-10 py-10 max-w-screen-xl mx-auto space-y-6">
+    <StudentLayout>
+      <div className="px-6 md:px-8 py-8 max-w-4xl mx-auto space-y-6">
 
-        {error && (
-          <div className="px-4 py-3 text-sm" style={{ border: '1px solid #DC2626', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
-            {error}
-          </div>
-        )}
+        {/* ── Back link ── */}
+        <button
+          onClick={() => navigate('/student/dashboard')}
+          className="flex items-center gap-2 text-xs font-medium opacity-50 hover:opacity-100 transition-opacity"
+          style={{ color: INK }}
+        >
+          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><path d="M19 12H5M12 5l-7 7 7 7"/></svg>
+          Back to Dashboard
+        </button>
 
-        {/* ── Status banner ── */}
+        {/* ── Hero card ── */}
         <div
-          className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 px-6 py-5"
-          style={{ backgroundColor: PRIMARY_LIGHT, border: BORDER }}
+          className="rounded-2xl p-6"
+          style={{ backgroundColor: WHITE, border: '1px solid #E2E8F0', boxShadow: '0 1px 4px rgba(0,0,0,0.05)' }}
         >
-          <div>
-            <h1 className="font-black text-2xl md:text-3xl tracking-tight leading-tight">{application?.campus_name}</h1>
-            <p className="text-sm opacity-55 mt-1">{application?.course_name}</p>
+          <div className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4 mb-5">
+            <div>
+              <p className="text-xs tracking-[0.2em] uppercase mb-1.5" style={{ color: PRIMARY }}>Application</p>
+              <h1 className="font-black text-2xl md:text-3xl tracking-tight leading-tight">{application?.campus_name}</h1>
+              <p className="text-sm opacity-50 mt-1">{application?.course_name} · {application?.country_name}</p>
+            </div>
+            <div className="flex flex-col items-end gap-2 shrink-0">
+              <StatusBadge status={application?.status} />
+              <p className="font-mono text-xs opacity-30">{application?.application_unique_number}</p>
+            </div>
           </div>
-          <StatusBadge status={application?.status} />
+
+          {/* Timeline */}
+          <StatusTimeline status={application?.status} />
         </div>
 
-        {/* ── Application info ── */}
-        <Section title="Application Information">
-          <InfoRow label="Application Number" value={application?.application_unique_number} />
-          <InfoRow label="Status"             value={application?.status} />
-          <InfoRow label="Country"            value={application?.country_name} />
-          <InfoRow label="Intake"             value={application?.intake_name} />
-          <InfoRow label="Campus Email"       value={application?.campus_email} />
-          <InfoRow
-            label="Applied On"
-            value={application?.created_at ? new Date(application.created_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' }) : undefined}
-          />
-        </Section>
-
-        {/* ── Student info ── */}
-        <Section title="Student Information">
-          <InfoRow label="Full Name" value={application?.student_name}  />
-          <InfoRow label="Email"     value={application?.student_email} />
-          <InfoRow label="Phone"     value={application?.student_phone} />
-        </Section>
-
-        {/* ── Documents ── */}
-        {application?.documents?.length > 0 && (
-          <Section title={`Documents (${application.documents.length})`}>
-            {application.documents.map((doc, i) => (
-              <div
-                key={doc.id}
-                className="flex items-center justify-between py-4 gap-4"
-                style={{ borderBottom: i < application.documents.length - 1 ? BORDER : 'none' }}
-              >
-                <div>
-                  <p className="font-semibold text-sm">{DOCUMENT_TYPES[doc.document_type] || doc.document_type}</p>
-                  <p className="text-xs opacity-40 mt-0.5">Uploaded {new Date(doc.uploaded_at).toLocaleDateString('en-GB')}</p>
-                </div>
-                <button
-                  onClick={() => handleViewDocument(doc.file_path)}
-                  className="text-xs px-4 py-2 font-semibold tracking-wide transition-colors shrink-0"
-                  style={{ border: `1px solid ${PRIMARY}`, color: PRIMARY, borderRadius: '6px' }}
-                  onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY; e.currentTarget.style.color = WHITE; }}
-                  onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = PRIMARY; }}
-                >
-                  View
-                </button>
-              </div>
-            ))}
-          </Section>
+        {error && (
+          <div className="px-4 py-3 rounded-xl text-sm" style={{ border: '1px solid #FCA5A5', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
+            {error}
+          </div>
         )}
 
-        {/* ── Actions ── */}
-        <div style={{ border: BORDER }}>
-          <div className="px-6 py-4" style={{ backgroundColor: PRIMARY_LIGHT, borderBottom: BORDER }}>
-            <h2 className="font-bold text-sm tracking-[0.15em] uppercase" style={{ color: PRIMARY }}>Actions</h2>
-          </div>
-          <div className="px-6 py-6 space-y-3">
+        {/* ── Layout: two columns on md+ ── */}
+        <div className="grid gap-6 md:grid-cols-2">
 
-            {application?.status === 'Awaiting Initial Payment' && (
-              <PrimaryBtn onClick={() => handlePayment('Initial')} disabled={actionLoading}>
-                {actionLoading ? 'Processing…' : 'Make Initial Payment (LKR 50,000)'}
-              </PrimaryBtn>
-            )}
+          {/* LEFT column */}
+          <div className="space-y-6">
 
-            {application?.status === 'Payment Completed' && (
-              <>
-                <div className="px-4 py-3 text-sm" style={{ border: `1px solid ${PRIMARY}`, backgroundColor: PRIMARY_LIGHT, color: PRIMARY }}>
-                  Download your application form, sign it, and upload along with required documents.
-                </div>
-                <PrimaryBtn onClick={handleDownloadForm} disabled={actionLoading}>
-                  {actionLoading ? 'Preparing PDF…' : 'Download Application Form'}
-                </PrimaryBtn>
-                <OutlineBtn onClick={() => navigate(`/student/applications/${id}`)}>Upload Documents</OutlineBtn>
-              </>
-            )}
+            {/* Application info */}
+            <Card title="Application Details">
+              <InfoRow label="App Number" value={application?.application_unique_number} />
+              <InfoRow label="Country"    value={application?.country_name} />
+              <InfoRow label="Campus"     value={application?.campus_name} />
+              <InfoRow label="Course"     value={application?.course_name} />
+              <InfoRow label="Intake"     value={application?.intake_name} />
+              <InfoRow label="Applied On" value={
+                application?.created_at
+                  ? new Date(application.created_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' })
+                  : undefined
+              } />
+              <InfoRow label="Campus Email" value={application?.campus_email} />
+            </Card>
 
-            {application?.status === 'Awaiting Admin Approval' && (
-              <div className="px-4 py-3 text-sm" style={{ border: '1px solid #0369A1', backgroundColor: '#F0F9FF', color: '#0369A1' }}>
-                Your documents are under review. You will be notified once the admin approves your application.
-              </div>
-            )}
+            {/* Student info */}
+            <Card title="Student Information">
+              <InfoRow label="Full Name" value={application?.student_name} />
+              <InfoRow label="Email"     value={application?.student_email} />
+              <InfoRow label="Phone"     value={application?.student_phone} />
+            </Card>
+          </div>
+
+          {/* RIGHT column */}
+          <div className="space-y-6">
 
-            {application?.status === 'Submitted to Campus' && (
-              <div className="px-4 py-3 text-sm" style={{ border: '1px solid #0369A1', backgroundColor: '#F0F9FF', color: '#0369A1' }}>
-                Your application has been forwarded to the campus. Waiting for offer letter.
+            {/* Action panel */}
+            <div
+              className="rounded-2xl p-5"
+              style={{ backgroundColor: actionCfg.bg || PRIMARY_LIGHT, border: `1px solid ${actionCfg.color || PRIMARY}40` }}
+            >
+              <div className="flex items-center gap-3 mb-3">
+                <span className="text-2xl">{actionCfg.icon}</span>
+                <h3 className="font-bold text-sm" style={{ color: actionCfg.color || PRIMARY }}>{actionCfg.title}</h3>
               </div>
-            )}
+              <p className="text-sm opacity-70 mb-4 leading-relaxed">{actionCfg.message}</p>
+
+              {/* Primary action buttons */}
+              {actionCfg.type === 'payment' && (
+                <button
+                  onClick={() => handlePayment('Initial')}
+                  disabled={actionLoading}
+                  className="w-full py-3.5 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85 disabled:opacity-50"
+                  style={{ backgroundColor: PRIMARY, color: WHITE }}
+                >
+                  {actionLoading ? 'Processing…' : actionCfg.primaryLabel}
+                </button>
+              )}
 
-            {application?.status === 'Offer Letter Received' && (
-              <>
-                <div className="px-4 py-3 text-sm" style={{ border: '1px solid #047857', backgroundColor: '#F0FDF4', color: '#047857' }}>
-                  Congratulations! You have received an offer letter. Proceed to final payment to confirm your place.
+              {actionCfg.type === 'download' && (
+                <div className="space-y-2.5">
+                  <button
+                    onClick={handleDownloadForm}
+                    disabled={actionLoading}
+                    className="w-full py-3.5 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85 disabled:opacity-50"
+                    style={{ backgroundColor: '#0EA5E9', color: WHITE }}
+                  >
+                    {actionLoading ? 'Preparing PDF…' : actionCfg.primaryLabel}
+                  </button>
+                  <button
+                    onClick={() => navigate(`/student/applications/${id}/upload`)}
+                    className="w-full py-3.5 text-sm font-semibold tracking-wide rounded-xl transition-colors"
+                    style={{ border: `1px solid #0EA5E9`, color: '#0EA5E9', backgroundColor: 'transparent' }}
+                    onMouseEnter={e => { e.currentTarget.style.backgroundColor = '#F0F9FF'; }}
+                    onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; }}
+                  >
+                    {actionCfg.secondaryLabel}
+                  </button>
                 </div>
-                <PrimaryBtn onClick={() => handlePayment('Final')} disabled={actionLoading}>
-                  {actionLoading ? 'Processing…' : 'Make Final Payment (LKR 100,000)'}
-                </PrimaryBtn>
-              </>
-            )}
+              )}
 
-            {application?.status === 'Completed' && (
-              <div className="px-4 py-3 text-sm" style={{ border: '1px solid #047857', backgroundColor: '#F0FDF4', color: '#047857' }}>
-                Your application process has been completed successfully. Congratulations!
-              </div>
-            )}
+              {actionCfg.type === 'upload' && (
+                <button
+                  onClick={() => navigate(`/student/applications/${id}/upload`)}
+                  className="w-full py-3.5 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85"
+                  style={{ backgroundColor: PRIMARY, color: WHITE }}
+                >
+                  {actionCfg.primaryLabel}
+                </button>
+              )}
 
-            {application?.status === 'Rejected' && (
-              <div className="px-4 py-3 text-sm" style={{ border: '1px solid #DC2626', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
-                Unfortunately, your application has been rejected. Please contact support for more information.
-              </div>
-            )}
+              {actionCfg.type === 'final-payment' && (
+                <button
+                  onClick={() => handlePayment('Final')}
+                  disabled={actionLoading}
+                  className="w-full py-3.5 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85 disabled:opacity-50"
+                  style={{ backgroundColor: '#059669', color: WHITE }}
+                >
+                  {actionLoading ? 'Processing…' : actionCfg.primaryLabel}
+                </button>
+              )}
+            </div>
+
+            {/* Documents */}
+            <Card title={`Documents ${application?.documents?.length ? `(${application.documents.length})` : ''}`}>
+              {!application?.documents?.length ? (
+                <div className="py-8 text-center">
+                  <p className="text-sm opacity-30">No documents uploaded yet.</p>
+                </div>
+              ) : (
+                <div>
+                  {application.documents.map((doc, i) => (
+                    <div
+                      key={doc.id}
+                      className="flex items-center justify-between py-3.5 gap-3"
+                      style={{ borderBottom: i < application.documents.length - 1 ? '1px solid #F1F5F9' : 'none' }}
+                    >
+                      <div className="min-w-0">
+                        <p className="font-semibold text-sm truncate">{DOCUMENT_TYPES[doc.document_type] || doc.document_type}</p>
+                        <p className="text-[11px] opacity-35 mt-0.5">
+                          {new Date(doc.uploaded_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })}
+                        </p>
+                      </div>
+                      <button
+                        onClick={() => handleViewDocument(doc.file_path)}
+                        className="text-xs px-3.5 py-2 font-semibold tracking-wide rounded-lg transition-colors shrink-0"
+                        style={{ border: `1px solid ${PRIMARY}`, color: PRIMARY }}
+                        onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY; e.currentTarget.style.color = WHITE; }}
+                        onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = PRIMARY; }}
+                      >
+                        View
+                      </button>
+                    </div>
+                  ))}
+                </div>
+              )}
+            </Card>
 
-            <OutlineBtn onClick={() => navigate('/student/dashboard')}>← Back to Dashboard</OutlineBtn>
           </div>
         </div>
-
       </div>
     </StudentLayout>
   );
diff --git a/src/pages/student/ApplicationWizard.jsx b/src/pages/student/ApplicationWizard.jsx
index 560ff10..b476da0 100644
--- a/src/pages/student/ApplicationWizard.jsx
+++ b/src/pages/student/ApplicationWizard.jsx
@@ -338,17 +338,17 @@ const ApplicationWizard = () => {
   };
 
   return (
-    <StudentLayout title="New Application">
+    <StudentLayout>
       <WizardProgress steps={APPLICATION_STEPS} currentStep={currentStep} />
 
-      <div className="px-6 md:px-10 py-10 max-w-2xl mx-auto">
+      <div className="px-6 md:px-8 py-8 max-w-2xl mx-auto">
         {error && (
-          <div className="mb-6 px-4 py-3 text-sm" style={{ border: '1px solid #DC2626', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
+          <div className="mb-6 px-4 py-3 text-sm rounded-xl" style={{ border: '1px solid #FCA5A5', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
             {error}
           </div>
         )}
 
-        <div style={{ border: BORDER }}>
+        <div className="rounded-2xl overflow-hidden" style={{ backgroundColor: '#FFFFFF', border: '1px solid #E2E8F0', boxShadow: '0 1px 4px rgba(0,0,0,0.05)' }}>
           <div className="px-6 py-5" style={{ backgroundColor: PRIMARY_LIGHT, borderBottom: BORDER }}>
             <h2 className="font-bold text-sm tracking-[0.15em] uppercase" style={{ color: PRIMARY }}>
               {APPLICATION_STEPS[currentStep - 1]?.name}
diff --git a/src/pages/student/Dashboard.jsx b/src/pages/student/Dashboard.jsx
index b8b52c9..f014f99 100644
--- a/src/pages/student/Dashboard.jsx
+++ b/src/pages/student/Dashboard.jsx
@@ -6,189 +6,325 @@ import StudentLayout from '../../layouts/StudentLayout';
 import { WHITE, INK, BORDER, PRIMARY, PRIMARY_LIGHT } from '../../utils/theme';
 import { STATUS_COLORS, STATUS_NEXT_ACTION } from '../../utils/constants';
 
-// ─── Status badge ─────────────────────────────────────────────────────────────
-const STATUS_TEXT_COLOR = {
-  pending: 'text-amber-700',
-  info:    'text-sky-700',
-  warning: 'text-orange-700',
-  success: 'text-emerald-700',
-  error:   'text-red-700',
+// ─── Status config ─────────────────────────────────────────────────────────────
+const STATUS_STYLE = {
+  pending: { bg: '#FFFBEB', border: '#FCD34D', text: '#92400E' },
+  info:    { bg: '#F0F9FF', border: '#7DD3FC', text: '#075985' },
+  warning: { bg: '#FFF7ED', border: '#FDBA74', text: '#9A3412' },
+  success: { bg: '#F0FDF4', border: '#86EFAC', text: '#14532D' },
+  error:   { bg: '#FEF2F2', border: '#FCA5A5', text: '#7F1D1D' },
 };
 
 const StatusBadge = ({ status }) => {
   const variant = STATUS_COLORS[status] || 'info';
+  const s = STATUS_STYLE[variant];
   return (
     <span
-      className={`text-xs px-2.5 py-1 tracking-wider uppercase font-semibold shrink-0 ${STATUS_TEXT_COLOR[variant]}`}
-      style={{ border: '1px solid currentColor' }}
+      className="text-[10px] px-2.5 py-1 font-bold tracking-widest uppercase rounded-full shrink-0"
+      style={{ backgroundColor: s.bg, border: `1px solid ${s.border}`, color: s.text }}
     >
       {status}
     </span>
   );
 };
 
-// ─── Single application card ──────────────────────────────────────────────────
-const ApplicationCard = ({ app, onView }) => (
-  <div style={{ border: BORDER }} className="bg-white">
-    <div className="flex items-start justify-between gap-4 px-6 py-5" style={{ borderBottom: BORDER }}>
-      <div className="min-w-0">
-        <h3 className="font-bold text-lg tracking-tight leading-tight truncate">{app.campus_name}</h3>
-        <p className="text-sm opacity-55 mt-0.5">{app.course_name}</p>
+// ─── Workflow step indicator ───────────────────────────────────────────────────
+const WORKFLOW = [
+  'Awaiting Initial Payment',
+  'Payment Completed',
+  'Awaiting Admin Approval',
+  'Submitted to Campus',
+  'Offer Letter Received',
+  'Awaiting Final Approval',
+  'Completed',
+];
+
+const WorkflowProgress = ({ status }) => {
+  if (status === 'Rejected') {
+    return (
+      <div className="flex items-center gap-2 mt-3">
+        <span className="w-2 h-2 rounded-full bg-red-500 shrink-0" />
+        <span className="text-xs text-red-600 font-medium">Application Rejected</span>
+      </div>
+    );
+  }
+  const currentIdx = WORKFLOW.indexOf(status);
+  const progress = currentIdx === -1 ? 0 : Math.round(((currentIdx + 1) / WORKFLOW.length) * 100);
+  return (
+    <div className="mt-3">
+      <div className="flex justify-between items-center mb-1.5">
+        <span className="text-[10px] tracking-widest uppercase opacity-40">Progress</span>
+        <span className="text-[10px] font-bold" style={{ color: PRIMARY }}>{progress}%</span>
+      </div>
+      <div className="h-1.5 rounded-full overflow-hidden" style={{ backgroundColor: `${PRIMARY}20` }}>
+        <div
+          className="h-full rounded-full transition-all duration-500"
+          style={{ width: `${progress}%`, backgroundColor: status === 'Completed' ? '#10B981' : PRIMARY }}
+        />
       </div>
-      <StatusBadge status={app.status} />
     </div>
+  );
+};
+
+// ─── Action icon ───────────────────────────────────────────────────────────────
+const ACTION_ICONS = {
+  'Awaiting Initial Payment':  '💳',
+  'Payment Completed':         '📄',
+  'Awaiting Document Upload':  '📤',
+  'Awaiting Admin Approval':   '⏳',
+  'Submitted to Campus':       '🏫',
+  'Offer Letter Received':     '🎉',
+  'Awaiting Final Approval':   '⏳',
+  'Completed':                 '✅',
+  'Rejected':                  '❌',
+};
+
+// ─── Application card ──────────────────────────────────────────────────────────
+const ApplicationCard = ({ app, onView }) => {
+  const [hovered, setHovered] = useState(false);
+  const isActionNeeded = ['Awaiting Initial Payment', 'Payment Completed', 'Awaiting Document Upload', 'Offer Letter Received'].includes(app.status);
 
-    <div className="grid grid-cols-3 px-6 py-4 text-sm" style={{ borderBottom: BORDER, backgroundColor: PRIMARY_LIGHT }}>
-      {[
-        { label: 'App Number', value: app.application_unique_number },
-        { label: 'Country',    value: app.country_name              },
-        { label: 'Intake',     value: app.intake_name               },
-      ].map(({ label, value }) => (
-        <div key={label}>
-          <p className="text-xs tracking-[0.12em] uppercase opacity-40 mb-0.5">{label}</p>
-          <p className="font-semibold truncate">{value}</p>
+  return (
+    <div
+      className="relative rounded-xl cursor-pointer transition-all duration-200"
+      style={{
+        backgroundColor: WHITE,
+        border: hovered ? `1.5px solid ${PRIMARY}` : `1px solid #E2E8F0`,
+        boxShadow: hovered ? `0 4px 20px ${PRIMARY}18` : '0 1px 4px rgba(0,0,0,0.06)',
+      }}
+      onClick={() => onView(app.id)}
+      onMouseEnter={() => setHovered(true)}
+      onMouseLeave={() => setHovered(false)}
+    >
+      {/* Action needed indicator */}
+      {isActionNeeded && (
+        <div
+          className="absolute top-3 right-3 w-2 h-2 rounded-full"
+          style={{ backgroundColor: PRIMARY }}
+          title="Action required"
+        />
+      )}
+
+      <div className="p-5">
+        {/* Header */}
+        <div className="flex items-start gap-3 mb-3">
+          <div
+            className="w-10 h-10 rounded-xl flex items-center justify-center text-lg shrink-0"
+            style={{ backgroundColor: PRIMARY_LIGHT }}
+          >
+            {ACTION_ICONS[app.status] || '📋'}
+          </div>
+          <div className="min-w-0 flex-1">
+            <h3 className="font-bold text-base leading-tight truncate" style={{ color: INK }}>{app.campus_name}</h3>
+            <p className="text-xs opacity-50 mt-0.5 truncate">{app.course_name}</p>
+          </div>
         </div>
-      ))}
-    </div>
 
-    <div className="px-6 py-3.5 text-sm" style={{ borderBottom: BORDER }}>
-      <span style={{ color: PRIMARY }} className="mr-2">→</span>
-      <span className="opacity-60">{STATUS_NEXT_ACTION[app.status]}</span>
+        {/* Meta grid */}
+        <div className="grid grid-cols-2 gap-x-4 gap-y-2.5 mb-3">
+          {[
+            { label: 'App No.', value: app.application_unique_number },
+            { label: 'Country', value: app.country_name },
+            { label: 'Intake',  value: app.intake_name },
+          ].map(({ label, value }) => (
+            <div key={label}>
+              <p className="text-[10px] tracking-widest uppercase opacity-35 mb-0.5">{label}</p>
+              <p className="text-xs font-semibold truncate">{value || '—'}</p>
+            </div>
+          ))}
+        </div>
+
+        {/* Progress bar */}
+        <WorkflowProgress status={app.status} />
+
+        {/* Divider */}
+        <div className="my-3" style={{ borderTop: '1px solid #F1F5F9' }} />
+
+        {/* Status + next action */}
+        <div className="flex items-center justify-between gap-3">
+          <StatusBadge status={app.status} />
+          <span
+            className="text-xs font-medium truncate"
+            style={{ color: isActionNeeded ? PRIMARY : `${INK}50` }}
+          >
+            {isActionNeeded && '→ '}{STATUS_NEXT_ACTION[app.status]}
+          </span>
+        </div>
+      </div>
     </div>
+  );
+};
 
-    <div className="px-6 py-4 flex flex-wrap gap-3">
-      <button
-        onClick={() => onView(app.id)}
-        className="text-xs px-5 py-2.5 font-semibold tracking-wide transition-colors"
-        style={{ border: `1px solid ${PRIMARY}`, color: PRIMARY, borderRadius: '6px' }}
-        onMouseEnter={e => { e.currentTarget.style.backgroundColor = PRIMARY; e.currentTarget.style.color = WHITE; }}
-        onMouseLeave={e => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = PRIMARY; }}
-      >
-        View Details
-      </button>
-      {app.status === 'Awaiting Initial Payment' && (
-        <button
-          onClick={() => onView(app.id)}
-          className="text-xs px-5 py-2.5 font-semibold tracking-wide transition-opacity hover:opacity-80"
-          style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '6px' }}
-        >
-          Make Payment
-        </button>
-      )}
-      {(app.status === 'Payment Completed' || app.status === 'Awaiting Document Upload') && (
-        <button
-          onClick={() => onView(app.id)}
-          className="text-xs px-5 py-2.5 font-semibold tracking-wide transition-opacity hover:opacity-80"
-          style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '6px' }}
-        >
-          Upload Documents
-        </button>
-      )}
-      {app.status === 'Offer Letter Received' && (
-        <button
-          onClick={() => onView(app.id)}
-          className="text-xs px-5 py-2.5 font-semibold tracking-wide transition-opacity hover:opacity-80"
-          style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '6px' }}
-        >
-          Review Offer
-        </button>
-      )}
+// ─── Stat card ─────────────────────────────────────────────────────────────────
+const StatCard = ({ label, value, accent, icon }) => (
+  <div
+    className="flex items-center gap-4 p-5 rounded-xl"
+    style={{ backgroundColor: WHITE, border: '1px solid #E2E8F0', boxShadow: '0 1px 4px rgba(0,0,0,0.05)' }}
+  >
+    <div
+      className="w-11 h-11 rounded-xl flex items-center justify-center text-xl shrink-0"
+      style={{ backgroundColor: `${accent}18` }}
+    >
+      {icon}
+    </div>
+    <div>
+      <p className="text-2xl font-black leading-none" style={{ color: accent }}>{value}</p>
+      <p className="text-xs opacity-50 mt-0.5 tracking-wide">{label}</p>
     </div>
   </div>
 );
 
-// ─── Page ─────────────────────────────────────────────────────────────────────
+// ─── Page ──────────────────────────────────────────────────────────────────────
 const StudentDashboard = () => {
-  const navigate = useNavigate();
-  const user     = getCurrentUser();
-
+  const navigate   = useNavigate();
+  const user       = getCurrentUser();
   const [applications, setApplications] = useState([]);
   const [loading, setLoading]           = useState(true);
   const [error, setError]               = useState('');
 
   useEffect(() => {
-    const fetchApplications = async () => {
-      try {
-        const response = await api.get('/applications');
-        setApplications(response.data.data);
-      } catch {
-        setError('Failed to load applications. Please refresh the page.');
-      } finally {
-        setLoading(false);
-      }
-    };
-    fetchApplications();
+    api.get('/applications')
+      .then(r => setApplications(r.data.data))
+      .catch(() => setError('Failed to load applications. Please refresh.'))
+      .finally(() => setLoading(false));
   }, []);
 
+  // Compute stats
+  const actionNeeded = applications.filter(a =>
+    ['Awaiting Initial Payment', 'Payment Completed', 'Awaiting Document Upload', 'Offer Letter Received'].includes(a.status)
+  ).length;
+  const inProgress = applications.filter(a =>
+    ['Awaiting Admin Approval', 'Submitted to Campus', 'Awaiting Final Approval'].includes(a.status)
+  ).length;
+  const completed = applications.filter(a => a.status === 'Completed').length;
+
   return (
-    <StudentLayout title="Dashboard">
-      {/* ── Hero greeting ── */}
-      <div className="px-6 md:px-10 py-10 md:py-14" style={{ backgroundColor: PRIMARY_LIGHT, borderBottom: BORDER }}>
-        <div className="max-w-screen-xl mx-auto flex flex-col sm:flex-row sm:items-end sm:justify-between gap-6">
+    <StudentLayout>
+      <div className="px-6 md:px-8 py-8 max-w-5xl mx-auto">
+
+        {/* ── Page header ── */}
+        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-8">
           <div>
-            <p className="text-xs tracking-[0.3em] uppercase mb-3" style={{ color: PRIMARY }}>Student Portal</p>
-            <h1 className="font-black text-3xl md:text-4xl tracking-tight leading-tight" style={{ color: INK }}>
-              Welcome back,<br />{user?.full_name?.split(' ')[0]}.
+            <p className="text-xs tracking-[0.25em] uppercase mb-1.5" style={{ color: PRIMARY }}>Student Portal</p>
+            <h1 className="font-black text-2xl md:text-3xl tracking-tight leading-tight" style={{ color: INK }}>
+              Welcome back, {user?.full_name?.split(' ')[0] || 'Student'} 👋
             </h1>
-            <p className="text-sm opacity-55 mt-2">Track your application journey to study abroad.</p>
+            <p className="text-sm opacity-50 mt-1">Here's your application overview.</p>
           </div>
           <button
             onClick={() => navigate('/student/apply')}
-            className="text-sm px-7 py-3.5 font-semibold tracking-wide transition-opacity hover:opacity-80 shrink-0"
-            style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '8px' }}
+            className="flex items-center gap-2 text-sm px-5 py-3 font-semibold tracking-wide transition-opacity hover:opacity-80 shrink-0 rounded-xl"
+            style={{ backgroundColor: PRIMARY, color: WHITE }}
           >
-            + New Application
+            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><path d="M12 5v14M5 12h14"/></svg>
+            New Application
           </button>
         </div>
-      </div>
 
-      {/* ── Content ── */}
-      <div className="px-6 md:px-10 py-10 max-w-screen-xl mx-auto">
         {error && (
-          <div className="mb-8 px-4 py-3 text-sm" style={{ border: '1px solid #DC2626', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
+          <div className="mb-6 px-4 py-3 text-sm rounded-lg" style={{ border: '1px solid #FCA5A5', backgroundColor: '#FEF2F2', color: '#DC2626' }}>
             {error}
           </div>
         )}
 
+        {/* ── Stats row ── */}
+        {!loading && applications.length > 0 && (
+          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
+            <StatCard label="Total"          value={applications.length} accent="#64748B"  icon="📋" />
+            <StatCard label="Action Needed"  value={actionNeeded}        accent={PRIMARY}   icon="⚡" />
+            <StatCard label="In Progress"    value={inProgress}          accent="#0EA5E9"  icon="⏳" />
+            <StatCard label="Completed"      value={completed}           accent="#10B981"  icon="✅" />
+          </div>
+        )}
+
+        {/* ── Applications ── */}
         {loading ? (
-          <div className="py-24 text-center">
-            <div className="inline-block w-8 h-8 border-2 border-t-transparent animate-spin" style={{ borderColor: PRIMARY, borderTopColor: 'transparent', borderRadius: '50%' }} />
-            <p className="mt-4 text-sm opacity-50">Loading applications…</p>
+          <div className="flex flex-col items-center justify-center py-24 gap-4">
+            <div
+              className="w-8 h-8 border-2 border-t-transparent rounded-full animate-spin"
+              style={{ borderColor: PRIMARY, borderTopColor: 'transparent' }}
+            />
+            <p className="text-sm opacity-40">Loading your applications…</p>
           </div>
+
         ) : applications.length === 0 ? (
-          <div className="py-24 text-center" style={{ border: BORDER }}>
-            <p className="text-xs tracking-[0.3em] uppercase opacity-30 mb-6">No applications yet</p>
-            <h2 className="font-black tracking-tight mb-6" style={{ fontSize: 'clamp(2rem, 5vw, 3.5rem)', lineHeight: '0.95' }}>
-              BEGIN YOUR<br />
-              <span style={{ WebkitTextStroke: `2px ${PRIMARY}`, color: 'transparent' }}>JOURNEY.</span>
+          <div className="flex flex-col items-center justify-center py-20 rounded-2xl text-center"
+            style={{ backgroundColor: WHITE, border: '2px dashed #E2E8F0' }}
+          >
+            <div className="text-5xl mb-4">🎓</div>
+            <h2 className="font-black text-2xl tracking-tight mb-2" style={{ color: INK }}>
+              Start Your Journey
             </h2>
-            <p className="text-sm opacity-55 max-w-xs mx-auto mb-8">
-              You haven't started any applications yet. Apply to a top university in minutes.
+            <p className="text-sm opacity-50 max-w-xs mb-8">
+              You haven't started any applications yet. Apply to a top university in just a few minutes.
             </p>
             <button
               onClick={() => navigate('/student/apply')}
-              className="text-sm px-8 py-4 font-semibold tracking-wide transition-opacity hover:opacity-80"
-              style={{ backgroundColor: PRIMARY, color: WHITE, borderRadius: '8px' }}
+              className="text-sm px-8 py-3.5 font-semibold tracking-wide transition-opacity hover:opacity-80 rounded-xl"
+              style={{ backgroundColor: PRIMARY, color: WHITE }}
             >
               Start My Application
             </button>
           </div>
+
         ) : (
-          <>
-            <div className="flex items-baseline justify-between mb-8">
-              <h2 className="font-black text-2xl tracking-tight uppercase">Applications</h2>
-              <p className="text-xs tracking-[0.2em] uppercase opacity-40">
-                {applications.length} {applications.length === 1 ? 'application' : 'applications'}
-              </p>
-            </div>
-            <div className="space-y-0" style={{ border: BORDER }}>
-              {applications.map((app, i) => (
-                <div key={app.id} style={{ borderTop: i === 0 ? 'none' : BORDER }}>
-                  <ApplicationCard app={app} onView={(id) => navigate(`/student/applications/${id}`)} />
+          <div>
+            {/* Action-needed section */}
+            {actionNeeded > 0 && (
+              <div className="mb-8">
+                <div className="flex items-center gap-2 mb-4">
+                  <span className="w-2 h-2 rounded-full" style={{ backgroundColor: PRIMARY }} />
+                  <h2 className="font-bold text-sm tracking-[0.1em] uppercase opacity-60">Action Required</h2>
+                  <span className="text-xs px-2 py-0.5 rounded-full font-bold text-white" style={{ backgroundColor: PRIMARY }}>{actionNeeded}</span>
                 </div>
-              ))}
-            </div>
-          </>
+                <div className="grid gap-4 sm:grid-cols-2">
+                  {applications
+                    .filter(a => ['Awaiting Initial Payment','Payment Completed','Awaiting Document Upload','Offer Letter Received'].includes(a.status))
+                    .map(app => (
+                      <ApplicationCard key={app.id} app={app} onView={id => navigate(`/student/applications/${id}`)} />
+                    ))
+                  }
+                </div>
+              </div>
+            )}
+
+            {/* In-progress section */}
+            {inProgress > 0 && (
+              <div className="mb-8">
+                <div className="flex items-center gap-2 mb-4">
+                  <span className="w-2 h-2 rounded-full bg-sky-500" />
+                  <h2 className="font-bold text-sm tracking-[0.1em] uppercase opacity-60">In Progress</h2>
+                  <span className="text-xs px-2 py-0.5 rounded-full font-bold text-white bg-sky-500">{inProgress}</span>
+                </div>
+                <div className="grid gap-4 sm:grid-cols-2">
+                  {applications
+                    .filter(a => ['Awaiting Admin Approval','Submitted to Campus','Awaiting Final Approval'].includes(a.status))
+                    .map(app => (
+                      <ApplicationCard key={app.id} app={app} onView={id => navigate(`/student/applications/${id}`)} />
+                    ))
+                  }
+                </div>
+              </div>
+            )}
+
+            {/* Completed / rejected section */}
+            {applications.filter(a => ['Completed','Rejected'].includes(a.status)).length > 0 && (
+              <div>
+                <div className="flex items-center gap-2 mb-4">
+                  <span className="w-2 h-2 rounded-full bg-slate-400" />
+                  <h2 className="font-bold text-sm tracking-[0.1em] uppercase opacity-60">Closed</h2>
+                </div>
+                <div className="grid gap-4 sm:grid-cols-2">
+                  {applications
+                    .filter(a => ['Completed','Rejected'].includes(a.status))
+                    .map(app => (
+                      <ApplicationCard key={app.id} app={app} onView={id => navigate(`/student/applications/${id}`)} />
+                    ))
+                  }
+                </div>
+              </div>
+            )}
+          </div>
         )}
       </div>
     </StudentLayout>
diff --git a/src/pages/student/Profile.jsx b/src/pages/student/Profile.jsx
new file mode 100644
index 0000000..64f46bc
--- /dev/null
+++ b/src/pages/student/Profile.jsx
@@ -0,0 +1,277 @@
+import { useState, useEffect } from 'react';
+import api from '../../services/api';
+import { getCurrentUser } from '../../services/auth';
+import StudentLayout from '../../layouts/StudentLayout';
+import { WHITE, INK, BORDER, PRIMARY, PRIMARY_LIGHT } from '../../utils/theme';
+
+// ─── Field ─────────────────────────────────────────────────────────────────────
+const Field = ({ label, type = 'text', value, onChange, placeholder, disabled, required }) => (
+  <div>
+    <label className="block text-xs font-semibold tracking-[0.15em] uppercase mb-2" style={{ color: `${INK}60` }}>
+      {label} {required && <span style={{ color: PRIMARY }}>*</span>}
+    </label>
+    <input
+      type={type}
+      value={value}
+      onChange={onChange}
+      placeholder={placeholder}
+      disabled={disabled}
+      className="w-full px-4 py-3 text-sm rounded-xl transition-colors outline-none disabled:opacity-40 disabled:cursor-not-allowed"
+      style={{
+        border: `1px solid #E2E8F0`,
+        backgroundColor: disabled ? '#F8FAFC' : WHITE,
+        fontFamily: "'Inter', system-ui, sans-serif",
+      }}
+      onFocus={e => { if (!disabled) { e.target.style.borderColor = PRIMARY; e.target.style.boxShadow = `0 0 0 3px ${PRIMARY}18`; } }}
+      onBlur={e => { e.target.style.borderColor = '#E2E8F0'; e.target.style.boxShadow = 'none'; }}
+    />
+  </div>
+);
+
+// ─── Card ──────────────────────────────────────────────────────────────────────
+const Card = ({ title, subtitle, children }) => (
+  <div className="rounded-2xl overflow-hidden" style={{ backgroundColor: WHITE, border: '1px solid #E2E8F0', boxShadow: '0 1px 4px rgba(0,0,0,0.05)' }}>
+    <div className="px-6 py-5" style={{ borderBottom: '1px solid #F1F5F9' }}>
+      <h2 className="font-bold text-sm">{title}</h2>
+      {subtitle && <p className="text-xs opacity-40 mt-0.5">{subtitle}</p>}
+    </div>
+    <div className="px-6 py-6">{children}</div>
+  </div>
+);
+
+// ─── Alert ─────────────────────────────────────────────────────────────────────
+const Alert = ({ type, message }) => {
+  if (!message) return null;
+  const cfg = type === 'success'
+    ? { bg: '#F0FDF4', border: '#86EFAC', color: '#14532D', icon: '✓' }
+    : { bg: '#FEF2F2', border: '#FCA5A5', color: '#7F1D1D', icon: '✕' };
+  return (
+    <div className="flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium"
+      style={{ backgroundColor: cfg.bg, border: `1px solid ${cfg.border}`, color: cfg.color }}>
+      <span className="font-bold">{cfg.icon}</span>
+      {message}
+    </div>
+  );
+};
+
+// ─── Page ──────────────────────────────────────────────────────────────────────
+const Profile = () => {
+  const user = getCurrentUser();
+
+  const [profile, setProfile]           = useState(null);
+  const [loading, setLoading]           = useState(true);
+
+  // Profile form state
+  const [fullName, setFullName]         = useState('');
+  const [phone, setPhone]               = useState('');
+  const [address, setAddress]           = useState('');
+  const [saving, setSaving]             = useState(false);
+  const [profileMsg, setProfileMsg]     = useState({ type: '', text: '' });
+
+  // Password form state
+  const [currentPwd, setCurrentPwd]     = useState('');
+  const [newPwd, setNewPwd]             = useState('');
+  const [confirmPwd, setConfirmPwd]     = useState('');
+  const [pwdSaving, setPwdSaving]       = useState(false);
+  const [pwdMsg, setPwdMsg]             = useState({ type: '', text: '' });
+
+  useEffect(() => {
+    api.get('/profile/student')
+      .then(r => {
+        const data = r.data.data;
+        setProfile(data);
+        setFullName(data.full_name || '');
+        setPhone(data.phone_number || '');
+        setAddress(data.address || '');
+      })
+      .catch(() => setProfileMsg({ type: 'error', text: 'Failed to load profile.' }))
+      .finally(() => setLoading(false));
+  }, []);
+
+  const handleSaveProfile = async (e) => {
+    e.preventDefault();
+    if (!fullName.trim()) {
+      setProfileMsg({ type: 'error', text: 'Full name is required.' });
+      return;
+    }
+    setSaving(true);
+    setProfileMsg({ type: '', text: '' });
+    try {
+      const r = await api.put('/profile/student', { full_name: fullName, phone_number: phone, address });
+      setProfile(r.data.data);
+      setProfileMsg({ type: 'success', text: 'Profile updated successfully.' });
+    } catch (err) {
+      setProfileMsg({ type: 'error', text: err.response?.data?.error || 'Failed to update profile.' });
+    } finally {
+      setSaving(false);
+    }
+  };
+
+  const handleChangePassword = async (e) => {
+    e.preventDefault();
+    if (!currentPwd || !newPwd || !confirmPwd) {
+      setPwdMsg({ type: 'error', text: 'All password fields are required.' });
+      return;
+    }
+    if (newPwd !== confirmPwd) {
+      setPwdMsg({ type: 'error', text: 'New passwords do not match.' });
+      return;
+    }
+    if (newPwd.length < 6) {
+      setPwdMsg({ type: 'error', text: 'New password must be at least 6 characters.' });
+      return;
+    }
+    setPwdSaving(true);
+    setPwdMsg({ type: '', text: '' });
+    try {
+      await api.put('/profile/student', { current_password: currentPwd, new_password: newPwd });
+      setPwdMsg({ type: 'success', text: 'Password changed successfully.' });
+      setCurrentPwd('');
+      setNewPwd('');
+      setConfirmPwd('');
+    } catch (err) {
+      setPwdMsg({ type: 'error', text: err.response?.data?.error || 'Failed to change password.' });
+    } finally {
+      setPwdSaving(false);
+    }
+  };
+
+  if (loading) {
+    return (
+      <StudentLayout>
+        <div className="flex items-center justify-center min-h-96">
+          <div className="text-center">
+            <div className="w-8 h-8 border-2 border-t-transparent rounded-full animate-spin mx-auto mb-4"
+              style={{ borderColor: PRIMARY, borderTopColor: 'transparent' }} />
+            <p className="text-sm opacity-40">Loading profile…</p>
+          </div>
+        </div>
+      </StudentLayout>
+    );
+  }
+
+  return (
+    <StudentLayout>
+      <div className="px-6 md:px-8 py-8 max-w-3xl mx-auto space-y-6">
+
+        {/* ── Page header ── */}
+        <div>
+          <p className="text-xs tracking-[0.25em] uppercase mb-1.5" style={{ color: PRIMARY }}>Account</p>
+          <h1 className="font-black text-2xl md:text-3xl tracking-tight" style={{ color: INK }}>My Profile</h1>
+          <p className="text-sm opacity-40 mt-1">Manage your personal information and password.</p>
+        </div>
+
+        {/* ── Avatar card ── */}
+        <div
+          className="flex items-center gap-5 p-5 rounded-2xl"
+          style={{ backgroundColor: PRIMARY_LIGHT, border: `1px solid ${PRIMARY}25` }}
+        >
+          <div
+            className="w-16 h-16 rounded-2xl flex items-center justify-center text-2xl font-black text-white shrink-0"
+            style={{ backgroundColor: PRIMARY }}
+          >
+            {(profile?.full_name || user?.full_name || 'S').charAt(0).toUpperCase()}
+          </div>
+          <div>
+            <p className="font-black text-lg leading-tight">{profile?.full_name || user?.full_name}</p>
+            <p className="text-sm opacity-50 mt-0.5">{profile?.email || user?.email}</p>
+            <p className="text-xs opacity-35 mt-1">
+              Member since {profile?.created_at
+                ? new Date(profile.created_at).toLocaleDateString('en-GB', { month: 'long', year: 'numeric' })
+                : '—'}
+            </p>
+          </div>
+        </div>
+
+        {/* ── Profile form ── */}
+        <Card title="Personal Information" subtitle="Update your name, phone number, and address.">
+          <form onSubmit={handleSaveProfile} className="space-y-5">
+            <Field
+              label="Full Name"
+              value={fullName}
+              onChange={e => setFullName(e.target.value)}
+              placeholder="Your full name"
+              required
+            />
+            <Field
+              label="Email Address"
+              type="email"
+              value={profile?.email || ''}
+              disabled
+              placeholder="your@email.com"
+            />
+            <div className="grid grid-cols-1 sm:grid-cols-2 gap-5">
+              <Field
+                label="Phone Number"
+                type="tel"
+                value={phone}
+                onChange={e => setPhone(e.target.value)}
+                placeholder="+94 77 000 0000"
+              />
+            </div>
+            <Field
+              label="Address"
+              value={address}
+              onChange={e => setAddress(e.target.value)}
+              placeholder="Your address"
+            />
+
+            <Alert type={profileMsg.type} message={profileMsg.text} />
+
+            <button
+              type="submit"
+              disabled={saving}
+              className="px-6 py-3 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85 disabled:opacity-50"
+              style={{ backgroundColor: PRIMARY, color: WHITE }}
+            >
+              {saving ? 'Saving…' : 'Save Changes'}
+            </button>
+          </form>
+        </Card>
+
+        {/* ── Password form ── */}
+        <Card title="Change Password" subtitle="Use a strong password to keep your account secure.">
+          <form onSubmit={handleChangePassword} className="space-y-5">
+            <Field
+              label="Current Password"
+              type="password"
+              value={currentPwd}
+              onChange={e => setCurrentPwd(e.target.value)}
+              placeholder="Enter current password"
+            />
+            <div className="grid grid-cols-1 sm:grid-cols-2 gap-5">
+              <Field
+                label="New Password"
+                type="password"
+                value={newPwd}
+                onChange={e => setNewPwd(e.target.value)}
+                placeholder="At least 6 characters"
+              />
+              <Field
+                label="Confirm New Password"
+                type="password"
+                value={confirmPwd}
+                onChange={e => setConfirmPwd(e.target.value)}
+                placeholder="Repeat new password"
+              />
+            </div>
+
+            <Alert type={pwdMsg.type} message={pwdMsg.text} />
+
+            <button
+              type="submit"
+              disabled={pwdSaving}
+              className="px-6 py-3 text-sm font-bold tracking-wide rounded-xl transition-opacity hover:opacity-85 disabled:opacity-50"
+              style={{ backgroundColor: INK, color: WHITE }}
+            >
+              {pwdSaving ? 'Changing…' : 'Change Password'}
+            </button>
+          </form>
+        </Card>
+
+      </div>
+    </StudentLayout>
+  );
+};
+
+export default Profile;
